apiVersion: v1
kind: PersistentVolume
metadata:
  name: app-pv
  namespace: app
spec:
  storageClassName: standard
  persistentVolumeReclaimPolicy: Delete
  accessModes:
  - ReadWriteMany
  capacity:
    storage: 1Gi
  hostPath:
    path: /app/data
